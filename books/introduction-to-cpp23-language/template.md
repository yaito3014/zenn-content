---
title: "テンプレート"
---

基本編では、`std::vector<int>` の `<int>` を名前の一部として使うにとどめた。この `<int>` は、`vector` に `int` を与えたものである。パラメータをとり、与えられたパラメータごとに具体的な関数・クラス・型エイリアスになる宣言を **テンプレート** と呼ぶ。テンプレートにできる宣言はほかにもあるが、本章では、型をパラメータにとる関数・クラス・型エイリアスのテンプレートを扱う。

## 関数テンプレート

基本編の関数は、`int` のような特定の型に対して書いた。同じ処理を別の型にも使いたいと、型ごとに書き直すことになる。テンプレートは、型をパラメータにして、その処理を1つだけ書く。

```cpp
template <typename T>
T twice(T x)
{
    return x + x;
}
```

`template <typename T>` は、`T` という型のパラメータを導入する。本体では `T` を型として使える。これを **関数テンプレート** と呼ぶ。

型パラメータの宣言では `class T` と書いても同じだが、この編は `typename` を使う。

呼び出すと、実引数の型から `T` が決まる。

```cpp
twice(5);     // T は int。10 を返す
twice(2.5);   // T は double。5.0 を返す
```

`twice(5)` では実引数 `5` が `int` なので `T` は `int` に、`twice(2.5)` では `double` になる。実引数から型パラメータが決まることを **実引数推論**(英語では deduction)と呼ぶ。`twice<int>(5)` と型を明示することもできるが、推論できるなら書かなくてよい。

もっとも、本体に書いた処理は、`T` がそれを行える型であることを暗黙に求める。`twice` の `x + x` は、`T` が `+` を使える型であることを前提とする。前提を満たさない型を与えるとどうなるかは、コンセプトの章で扱う。

## クラステンプレート

クラスも、型をパラメータにできる。

```cpp
template <typename T>
struct Box
{
    T value;
};
```

これを **クラステンプレート** と呼ぶ。使うときは、`Box<int>` のように型を与える。

```cpp
Box<int> a = {.value = 3};
Box<double> b = {.value = 2.5};
```

`Box<int>` は `value` が `int`、`Box<double>` は `value` が `double` になる。基本編で名前の一部として使った `std::vector<int>` の `<int>` も、これと同じく、クラステンプレート `vector` に型 `int` を与えたものである。標準ライブラリが提供するものの多くは、こうしたテンプレートである。

:::details クラステンプレートの型引数の推論
クラステンプレートでも、初期化子から型引数を推論できる場合がある(クラステンプレートの実引数推論、C++17 以降)。本章では `Box<int>` と型を明示する形を使う。
:::

## エイリアステンプレート

型エイリアスも、型をパラメータにできる。

```cpp
template <typename T>
using Ptr = T*;
```

`Ptr<int>` は `int*`、`Ptr<double>` は `double*` の別名になる。`T*` は、基本編の参照とポインタの章で見たポインタ型である。型エイリアスの章で見た `using` に、テンプレートのパラメータを加えた形である。
